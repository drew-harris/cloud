{
  "name": "drewh-cloud",
  "version": "1.0.0",
  "description": "CLI for Drewh Cloud Enterprises Hosting",
  "bin": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --packages=external"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.20.2",
    "esbuild-register": "^3.5.0",
    "tsconfig": "workspace:*",
    "typescript": "^5.4.5"
  },
  "keywords": [
    "CLI"
  ],
  "author": "Drew Harris",
  "license": "ISC"
}
